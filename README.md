# docker-template
A template to deploy a development container quickly


Super simple dockerfile and docker-compose.yml that allows you to deploy a simple nodejs dev environment.

Currently this only runs node and zsh. I'll eventually build an other docker compose to match my prodction environment